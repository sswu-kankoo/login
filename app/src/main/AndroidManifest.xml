<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kankoo.kankoo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainpage2"></activity>
        <activity android:name=".mainpage1" />
        <activity android:name=".RegistChoose" />
        <activity android:name=".RegistActivity1_auth_dialog" />
        <activity android:name=".RegistActivity1_auth" />
        <activity android:name=".RegistActivity2" />
        <activity android:name=".RegistActivity1" />
        <activity android:name=".QRgen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>